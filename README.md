# jemacs-stephen-config

Stephen's personal jemacs configuration. Loaded via `~/.jemacs/init.ts` (symlinked by `deploy.sh`).

Exports `install(editor)` — theme, vertico, keys, and experimental packages from `jemacs-packages`.

Requires `JEMACS_HOME` pointing at a jemacs checkout (set by the deploy wrapper).
